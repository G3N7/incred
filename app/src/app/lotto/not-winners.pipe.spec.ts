import { NotWinnersPipe } from './not-winners.pipe';

describe('NotWinnersPipe', () => {
  it('create an instance', () => {
    const pipe = new NotWinnersPipe();
    expect(pipe).toBeTruthy();
  });
});
